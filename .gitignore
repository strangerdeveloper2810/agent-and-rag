node_modules
dist
dist-ssr
.env
.env.local
*-credentials.env
# Cho phép env dev (safe defaults) — file production env/.env vẫn bị ignore
!env/.env.development
# Seed local cho deploy/deploy-to-vps.sh — chứa API key thật + secret hạ tầng
# đã sinh, KHÔNG BAO GIỜ commit (xem deploy/README.md).
env/.env.production
env/.env.production.secrets
.turbo
*.log
.DS_Store
coverage

# Tài liệu kiến trúc cá nhân (không commit vào repo chung)
/ARCHITECTURE_BFF_AGENT_GO.md
/ARCHITECTURE_DEEP_DIVE/

# Claude Code worktrees (mỗi thư mục là một git worktree riêng, không commit)
.claude/worktrees/
/ARCHITECTURE_DEEP_DIVE/
