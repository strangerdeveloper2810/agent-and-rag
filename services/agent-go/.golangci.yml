# golangci-lint config. Default linters (errcheck, govet, ineffassign,
# staticcheck, unused) + vài linter nhẹ. Target Go lấy theo go.mod.

run:
  timeout: 5m

linters:
  enable:
    - misspell
    - unconvert
    - bodyclose
