{
  "name": "@app/agent-go",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/server",
    "build": "go build -o bin/server ./cmd/server",
    "test": "go test ./...",
    "lint": "golangci-lint run || true",
    "typecheck": "go vet ./..."
  }
}
